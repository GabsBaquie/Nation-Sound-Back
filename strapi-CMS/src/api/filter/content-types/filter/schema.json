{
  "kind": "collectionType",
  "collectionName": "filters",
  "info": {
    "singularName": "filter",
    "pluralName": "filters",
    "displayName": "Filter",
    "description": "Collection pour gérer les différents types de filtres avec leurs valeurs."
  },
  "options": {
    "draftAndPublish": true
  },
  "pluginOptions": {},
  "attributes": {
    "name": {
      "type": "string",
      "required": true,
      "unique": true
    },
    "type": {
      "type": "enumeration",
      "enum": ["Type", "Importance", "Scène", "Catégorie"],
      "required": true
    },
    "value": {
      "type": "string",
      "required": true
    }
  },
  "config": {
    "mainField": "name"
  }
}
